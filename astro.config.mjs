// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
    site: "https://hosota9.github.io",
    image: {
        layout: "constrained",
    },
});
